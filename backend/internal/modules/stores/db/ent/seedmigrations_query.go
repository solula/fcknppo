// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"
	"waterfall-backend/internal/modules/stores/db/ent/predicate"
	"waterfall-backend/internal/modules/stores/db/ent/seedmigrations"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SeedMigrationsQuery is the builder for querying SeedMigrations entities.
type SeedMigrationsQuery struct {
	config
	ctx        *QueryContext
	order      []seedmigrations.Order
	inters     []Interceptor
	predicates []predicate.SeedMigrations
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SeedMigrationsQuery builder.
func (smq *SeedMigrationsQuery) Where(ps ...predicate.SeedMigrations) *SeedMigrationsQuery {
	smq.predicates = append(smq.predicates, ps...)
	return smq
}

// Limit the number of records to be returned by this query.
func (smq *SeedMigrationsQuery) Limit(limit int) *SeedMigrationsQuery {
	smq.ctx.Limit = &limit
	return smq
}

// Offset to start from.
func (smq *SeedMigrationsQuery) Offset(offset int) *SeedMigrationsQuery {
	smq.ctx.Offset = &offset
	return smq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (smq *SeedMigrationsQuery) Unique(unique bool) *SeedMigrationsQuery {
	smq.ctx.Unique = &unique
	return smq
}

// Order specifies how the records should be ordered.
func (smq *SeedMigrationsQuery) Order(o ...seedmigrations.Order) *SeedMigrationsQuery {
	smq.order = append(smq.order, o...)
	return smq
}

// First returns the first SeedMigrations entity from the query.
// Returns a *NotFoundError when no SeedMigrations was found.
func (smq *SeedMigrationsQuery) First(ctx context.Context) (*SeedMigrations, error) {
	nodes, err := smq.Limit(1).All(setContextOp(ctx, smq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{seedmigrations.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (smq *SeedMigrationsQuery) FirstX(ctx context.Context) *SeedMigrations {
	node, err := smq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SeedMigrations ID from the query.
// Returns a *NotFoundError when no SeedMigrations ID was found.
func (smq *SeedMigrationsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = smq.Limit(1).IDs(setContextOp(ctx, smq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{seedmigrations.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (smq *SeedMigrationsQuery) FirstIDX(ctx context.Context) int {
	id, err := smq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SeedMigrations entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SeedMigrations entity is found.
// Returns a *NotFoundError when no SeedMigrations entities are found.
func (smq *SeedMigrationsQuery) Only(ctx context.Context) (*SeedMigrations, error) {
	nodes, err := smq.Limit(2).All(setContextOp(ctx, smq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{seedmigrations.Label}
	default:
		return nil, &NotSingularError{seedmigrations.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (smq *SeedMigrationsQuery) OnlyX(ctx context.Context) *SeedMigrations {
	node, err := smq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SeedMigrations ID in the query.
// Returns a *NotSingularError when more than one SeedMigrations ID is found.
// Returns a *NotFoundError when no entities are found.
func (smq *SeedMigrationsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = smq.Limit(2).IDs(setContextOp(ctx, smq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{seedmigrations.Label}
	default:
		err = &NotSingularError{seedmigrations.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (smq *SeedMigrationsQuery) OnlyIDX(ctx context.Context) int {
	id, err := smq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SeedMigrationsSlice.
func (smq *SeedMigrationsQuery) All(ctx context.Context) ([]*SeedMigrations, error) {
	ctx = setContextOp(ctx, smq.ctx, "All")
	if err := smq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SeedMigrations, *SeedMigrationsQuery]()
	return withInterceptors[[]*SeedMigrations](ctx, smq, qr, smq.inters)
}

// AllX is like All, but panics if an error occurs.
func (smq *SeedMigrationsQuery) AllX(ctx context.Context) []*SeedMigrations {
	nodes, err := smq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SeedMigrations IDs.
func (smq *SeedMigrationsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if smq.ctx.Unique == nil && smq.path != nil {
		smq.Unique(true)
	}
	ctx = setContextOp(ctx, smq.ctx, "IDs")
	if err = smq.Select(seedmigrations.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (smq *SeedMigrationsQuery) IDsX(ctx context.Context) []int {
	ids, err := smq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (smq *SeedMigrationsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, smq.ctx, "Count")
	if err := smq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, smq, querierCount[*SeedMigrationsQuery](), smq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (smq *SeedMigrationsQuery) CountX(ctx context.Context) int {
	count, err := smq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (smq *SeedMigrationsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, smq.ctx, "Exist")
	switch _, err := smq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (smq *SeedMigrationsQuery) ExistX(ctx context.Context) bool {
	exist, err := smq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SeedMigrationsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (smq *SeedMigrationsQuery) Clone() *SeedMigrationsQuery {
	if smq == nil {
		return nil
	}
	return &SeedMigrationsQuery{
		config:     smq.config,
		ctx:        smq.ctx.Clone(),
		order:      append([]seedmigrations.Order{}, smq.order...),
		inters:     append([]Interceptor{}, smq.inters...),
		predicates: append([]predicate.SeedMigrations{}, smq.predicates...),
		// clone intermediate query.
		sql:  smq.sql.Clone(),
		path: smq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Migrated int `json:"migrated,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SeedMigrations.Query().
//		GroupBy(seedmigrations.FieldMigrated).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (smq *SeedMigrationsQuery) GroupBy(field string, fields ...string) *SeedMigrationsGroupBy {
	smq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SeedMigrationsGroupBy{build: smq}
	grbuild.flds = &smq.ctx.Fields
	grbuild.label = seedmigrations.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Migrated int `json:"migrated,omitempty"`
//	}
//
//	client.SeedMigrations.Query().
//		Select(seedmigrations.FieldMigrated).
//		Scan(ctx, &v)
func (smq *SeedMigrationsQuery) Select(fields ...string) *SeedMigrationsSelect {
	smq.ctx.Fields = append(smq.ctx.Fields, fields...)
	sbuild := &SeedMigrationsSelect{SeedMigrationsQuery: smq}
	sbuild.label = seedmigrations.Label
	sbuild.flds, sbuild.scan = &smq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SeedMigrationsSelect configured with the given aggregations.
func (smq *SeedMigrationsQuery) Aggregate(fns ...AggregateFunc) *SeedMigrationsSelect {
	return smq.Select().Aggregate(fns...)
}

func (smq *SeedMigrationsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range smq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, smq); err != nil {
				return err
			}
		}
	}
	for _, f := range smq.ctx.Fields {
		if !seedmigrations.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if smq.path != nil {
		prev, err := smq.path(ctx)
		if err != nil {
			return err
		}
		smq.sql = prev
	}
	if seedmigrations.Policy == nil {
		return errors.New("ent: uninitialized seedmigrations.Policy (forgotten import ent/runtime?)")
	}
	if err := seedmigrations.Policy.EvalQuery(ctx, smq); err != nil {
		return err
	}
	return nil
}

func (smq *SeedMigrationsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SeedMigrations, error) {
	var (
		nodes = []*SeedMigrations{}
		_spec = smq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SeedMigrations).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SeedMigrations{config: smq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(smq.modifiers) > 0 {
		_spec.Modifiers = smq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, smq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (smq *SeedMigrationsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := smq.querySpec()
	if len(smq.modifiers) > 0 {
		_spec.Modifiers = smq.modifiers
	}
	_spec.Node.Columns = smq.ctx.Fields
	if len(smq.ctx.Fields) > 0 {
		_spec.Unique = smq.ctx.Unique != nil && *smq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, smq.driver, _spec)
}

func (smq *SeedMigrationsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(seedmigrations.Table, seedmigrations.Columns, sqlgraph.NewFieldSpec(seedmigrations.FieldID, field.TypeInt))
	_spec.From = smq.sql
	if unique := smq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if smq.path != nil {
		_spec.Unique = true
	}
	if fields := smq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, seedmigrations.FieldID)
		for i := range fields {
			if fields[i] != seedmigrations.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := smq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := smq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := smq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := smq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (smq *SeedMigrationsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(smq.driver.Dialect())
	t1 := builder.Table(seedmigrations.Table)
	columns := smq.ctx.Fields
	if len(columns) == 0 {
		columns = seedmigrations.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if smq.sql != nil {
		selector = smq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if smq.ctx.Unique != nil && *smq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range smq.modifiers {
		m(selector)
	}
	for _, p := range smq.predicates {
		p(selector)
	}
	for _, p := range smq.order {
		p(selector)
	}
	if offset := smq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := smq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (smq *SeedMigrationsQuery) Modify(modifiers ...func(s *sql.Selector)) *SeedMigrationsSelect {
	smq.modifiers = append(smq.modifiers, modifiers...)
	return smq.Select()
}

// SeedMigrationsGroupBy is the group-by builder for SeedMigrations entities.
type SeedMigrationsGroupBy struct {
	selector
	build *SeedMigrationsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (smgb *SeedMigrationsGroupBy) Aggregate(fns ...AggregateFunc) *SeedMigrationsGroupBy {
	smgb.fns = append(smgb.fns, fns...)
	return smgb
}

// Scan applies the selector query and scans the result into the given value.
func (smgb *SeedMigrationsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, smgb.build.ctx, "GroupBy")
	if err := smgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeedMigrationsQuery, *SeedMigrationsGroupBy](ctx, smgb.build, smgb, smgb.build.inters, v)
}

func (smgb *SeedMigrationsGroupBy) sqlScan(ctx context.Context, root *SeedMigrationsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(smgb.fns))
	for _, fn := range smgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*smgb.flds)+len(smgb.fns))
		for _, f := range *smgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*smgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := smgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SeedMigrationsSelect is the builder for selecting fields of SeedMigrations entities.
type SeedMigrationsSelect struct {
	*SeedMigrationsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sms *SeedMigrationsSelect) Aggregate(fns ...AggregateFunc) *SeedMigrationsSelect {
	sms.fns = append(sms.fns, fns...)
	return sms
}

// Scan applies the selector query and scans the result into the given value.
func (sms *SeedMigrationsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sms.ctx, "Select")
	if err := sms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeedMigrationsQuery, *SeedMigrationsSelect](ctx, sms.SeedMigrationsQuery, sms, sms.inters, v)
}

func (sms *SeedMigrationsSelect) sqlScan(ctx context.Context, root *SeedMigrationsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sms.fns))
	for _, fn := range sms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sms *SeedMigrationsSelect) Modify(modifiers ...func(s *sql.Selector)) *SeedMigrationsSelect {
	sms.modifiers = append(sms.modifiers, modifiers...)
	return sms
}
